// @ts-check

/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  // Add this line to enable the standalone output
  output: "standalone",
}
 
module.exports = nextConfig